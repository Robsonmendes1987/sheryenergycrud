import { Router, Request, Response} from "express";
// import LoginController from "../controller/login.controller";

const route = Router()

// const  login = new loginController()

// route.post('/', (req: Request, res: Response) => new LoginController(req, res).login())
// route.get('/', (req: Request, res: Response) => new LoginController(req, res).validate())


export default route;